components:
  schemas:
    BadRequestErrorResponseBody:
      required:
        - error
      properties:
        timestamp:
          type: string
          format: date-time
        status:
          type: integer
        error:
          type: string
        message:
          type: string
