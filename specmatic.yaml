version: 3
systemUnderTest:
  service:
    $ref: "#/components/services/orderService"
    runOptions:
      $ref: "#/components/runOptions/orderServiceTest"
    settings:
      schemaResiliencyTests: all
specmatic:
  governance:
    successCriteria:
      minCoveragePercentage: 70
      maxMissedOperationsInSpec: 4
      enforce: true
components:
  sources:
    localContracts:
      filesystem:
        directory: .
  services:
    orderService:
      description: Order Service with OAuth
      definitions:
        - definition:
            source:
              $ref: "#/components/sources/localContracts"
            specs:
              - spec:
                  id: orderWithOAuthSpec
                  path: ./spec/api_order_with_oauth_v3.yaml
  runOptions:
    orderServiceTest:
      openapi:
        type: test
        filter: "PATH!='/internal/metrics'"
        baseUrl: ${APP_BASE_URL:http://localhost:8080}
        actuatorUrl: http://localhost:8080/actuator/mappings
        specs:
          - spec:
              id: orderWithOAuthSpec
              securitySchemes:
                oAuth2AuthCode:
                  type: oauth2
                  token: ${OAUTH_TOKEN:OAUTH1234}
